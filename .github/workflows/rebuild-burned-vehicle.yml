name: Rebuild burned vehicle asset
on:
  push:
    branches: [main]
    paths:
      - 'assets/staging/READY'
permissions:
  contents: write
jobs:
  rebuild:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Rebuild JPEG
        shell: bash
        run: |
          set -euo pipefail
          cat assets/staging/part-* > /tmp/burned.b64
          base64 -d /tmp/burned.b64 > assets/evidence/burned-vehicle-roadside-hd.jpg
          test "$(wc -c < assets/evidence/burned-vehicle-roadside-hd.jpg)" = "111213"
          echo "003525c9589373fb38851e44ae27946b60633642babdc8275fb5cb28b6efd422  assets/evidence/burned-vehicle-roadside-hd.jpg" | sha256sum -c -
          rm -rf assets/staging
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add assets/evidence/burned-vehicle-roadside-hd.jpg assets/staging
          git commit -m "Rebuild real burned vehicle JPEG asset"
          git push
